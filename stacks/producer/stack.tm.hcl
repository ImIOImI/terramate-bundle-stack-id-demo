stack {
  # Hand-seeded to equal bundle.let.producer_id (tm_uuidv5("dns","producer")).
  # This whole file is the workaround: `define bundle stack metadata { id = ... }`
  # would let the bundle set it instead, and this seed could be deleted.
  id = "2e2ea0f7-596a-57d6-b233-6deecf9941d5"
}
